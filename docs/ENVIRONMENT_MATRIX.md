# Environment matrix

Updated: 2026-09-17

This matrix is a compatibility register, not evidence that an integration works. Addresses, package versions, chain IDs, credentials, and service support remain `UNVERIFIED` until a bounded probe records a result in `EVIDENCE.md`.

| Integration | Intended environment | Version / source pin | Credentials needed | Test performed | Result | Artifact / next proof |
|---|---|---|---|---|---|---|
| Monad platform | Monad Testnet, chain ID `10143` (`0x279f`) | Foundation RPC `https://rpc-testnet.monadinfra.com`; official docs reviewed 2026-09-15 | No credential required for bounded public reads | `eth_chainId`, `eth_blockNumber`, `eth_gasPrice`, `eth_getCode` on canonical Multicall3 | VERIFIED | 2026-09-15 JSON-RPC responses: `0x279f`, `0x3ba6463`, `0x17bfac7c00`, non-empty Multicall3 bytecode; next: use an app-configured RPC before UI work |
| Kuru | Monad Testnet MON-USDC market `0xa241896A7Dbe8a550D2E5fF7A914bB1989ceD2D9`; USDC `0x3bA3d39AFcf8bb994f7964B3e0171Ea2Ba361570`; local fork chain `31337` | Contracts `2060bb2736080c175d80d568bfdb6226bb5abd04`; SDK/ABI `636509c2eafd63479d3f399703354e0d09f51e18`; fork source block `62944132`, hash `0xb5a5a556...c914098`; deployed implementation remains source-unverified | Public Foundation RPC supports the fixed-block fork; optional `MONAD_RPC_URL` overrides it locally | ABI-compatible reads, fork probe, and 5-case adapter settlement suite with labeled local mutations | PARTIAL PASS (behavioral fork settlement verified; source equivalence/public deployment blocked) | Actual input/output, partial refund, native MON, minOut, FOK, policy minFill/price rollback, nonce/allowance, pre-existing balances, zero new residual, and no active resting taker order verified. Need exact compiler input/deployment manifest including Solady revision |
| Privy | Web/PWA embedded EVM wallet; Monad Testnet chain ID `10143` is the sole default/supported chain | `@privy-io/react-auth` `3.42.0`; viem `2.56.5`; official network/send-transaction and allowed-origin docs reviewed 2026-09-17 | Public App ID and web Client ID are configured only in ignored `apps/web/.env.local`; app secret is not required by this client path; public Kairos policy/receiver addresses remain absent | Real SDK provider/action boundary compiles; 5 decoded transaction-builder cases; 14 web files / 39 tests; local runtime loaded chain `10143` but remained at session initialization before the Client ID/dashboard correction | PARTIAL PASS / BLOCKED FOR PRIVY-01 | Dashboard currently has email and external-wallet login enabled, no allowed origin, and automatic embedded-wallet creation disabled. Add `http://localhost:3000`, enable automatic EVM wallets, then rerun login/address/native-balance proof; lifecycle transactions still require a real limited deployment |
| CRE | Workflow plus Kairos receiver; Monad Testnet selector `2183018362218727504`; authenticated tenant production forwarder `0xF8344CFd5c43616a4366C34E3EEE75af79a74482`; mock forwarder `0xB9F79d863261869B234c481D1f9A7af84AeAd192` | CLI `v1.34.0`; `@chainlink/cre-sdk` `1.21.1`; workflow TypeScript `5.9.3`; SDK source `8a9e735c9046fef4356046b0d4b4c760089630df`; templates `d0223f31182c76bc36b1cc9d47b13b18efcf2bf6`; chain selectors `425da86147b75ee0fdd0d95d840a0966b837056b` | Local CLI authentication works; no linked owner key; deployment access remains unavailable; no workflow identity or public Kairos receiver exists | Receiver suite; 5 workflow tests; authenticated no-broadcast simulator rerun; non-empty forwarder bytecode check at block `63115376` | PARTIAL PASS | Real Kuru/fixture-policy WAIT measured `927ms`; fixture policy/L2 report generation measured `647ms`; both `submitted:false`. Simulation did not require a linked owner or production registry. Bytecode presence is not delivery proof. Need public policy/receiver and workflow identity only for deployed delivery proof |
| Kairos application index and reports | Monad Testnet `10143`; configured immutable policy/receiver; local atomic JSON projection | viem `2.56.5`; index schema `1`; Next.js `16.3.5` server read model | Public policy/receiver/market addresses, deployment block, `KAIROS_INDEX_PATH`; optional decision and execution-attempt journal paths | Event projection/restart/reorg tests; separate decision/attempt restart tests; dynamic order/detail/report routes; pinned current/historical reads compile; wallet attempt restart test; missing-config runtime gates inspected | PARTIAL PASS / BLOCKED FOR LIVE INGESTION | Local recovery, report accounting, failed-attempt states, and provenance pass. Need public Kairos deployment, real index state, and verified CRE execution-history producer before UI-01/live-report proof; browser wallet history remains local evidence only |
| Aurora Intents | User-wallet funding route; Monad Testnet/Kuru-USDC route `UNVERIFIED` | Current API guide documents app-key token discovery, dry quote, deposit, lifecycle and refund fields | `AURORA_API_KEY` missing locally | Current primary docs and M0.3 presence-only inventory; no API request made | BLOCKED | Authenticate, discover exact supported assets/networks, then perform a non-funding dry quote before any separately authorized test flow |
| Optional infrastructure | QuickNode, Tenderly, Zerion; none selected | User-provided Metropolis catalog, 2026-09-14 | Voucher/account only after user claims it | None | NOT SELECTED | Confirm entitlement, limits, cost, and Monad support before adding as a dependency |

## Source handling rules

- The Metropolis catalog is user-provided contextual material. It introduces no new sponsor requirement, implementation dependency, or verified capability.
- Do not claim a perk, API, RPC, contract address, network, token, ABI, or transaction until a runtime probe records the exact result in `EVIDENCE.md`.
- Submitted, confirmed, failed, and stale are distinct UI and evidence states. A no-pending-state marketing phrase does not override this.
